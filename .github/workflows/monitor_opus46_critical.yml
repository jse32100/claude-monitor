name: Opus 4.6 Critical Score Monitor

on:
  workflow_dispatch:

concurrency:
  group: opus46-critical-monitor
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  monitor:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests

      - name: Run monitor
        env:
          DISCORD_WEBHOOK_OPUS46_CRITICAL: ${{ secrets.DISCORD_WEBHOOK_OPUS46_CRITICAL }}
        run: python monitor_opus46_critical.py

      - name: Commit updated state
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add state_opus46_critical.json
          git diff --staged --quiet || git commit -m "chore: update opus4.6 critical state"
          git pull --rebase
          git push
